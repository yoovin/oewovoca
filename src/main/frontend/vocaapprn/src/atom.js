import {atom} from 'recoil'

export const voca = atom({
    key: 'voca',
    default: []
})

export const date = atom({
    key: 'date',
    default: ''
})

export const today = atom({
    key: 'today',
    default: ''
})