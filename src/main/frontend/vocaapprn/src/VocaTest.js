import { SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function VocaTest() {
    return (
        <SafeAreaView>
            <Text>대충 단어 시험 보는 페이지</Text>
        </SafeAreaView>
    )
}

const styles = StyleSheet.create({})